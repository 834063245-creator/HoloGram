# Copyright (c) 2026 Wenbing Jing. MIT License.
# SPDX-License-Identifier: MIT

# CI is disabled — engine source no longer public.
# Builds happen locally. Binaries published via GitHub Releases.
# See scripts/release.sh for local build + upload workflow.

name: CI (no-op)

on:
  push:
    branches: [main]

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI skipped — binary-only distribution. Build locally with scripts/release.sh"
