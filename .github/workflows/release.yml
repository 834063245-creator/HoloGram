# Copyright (c) 2026 Wenbing Jing. MIT License.
# SPDX-License-Identifier: MIT

# Release is done locally via scripts/release.sh — builds engine + app + uploads.
# This workflow is a no-op placeholder.

name: Release (no-op)

on:
  push:
    branches: [main]

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release skipped — binary-only distribution. Use scripts/release.sh locally"
