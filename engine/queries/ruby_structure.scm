;; Ruby Structure Queries

(method) @fn
(class) @class
(module) @class
(call) @call
(command_call) @call

;; ── Variables ──
(assignment) @var

;; ── Writes ──
(assignment) @write
(operator_assignment) @write

;; ── Usage ──
(identifier) @usage
