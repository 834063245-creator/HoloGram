---
description: TypeScript 前端结构与 Agent 联动约定
globs: src-ui/**/*
alwaysApply: false
---

# 前端 (`src-ui/`)

> ⚠️ 「观测台」重构进行中——**以 `docs/frontend-refactor-handoff.md` 为唯一事实来源**（含阶段进度/铁律/契约速查）。

**入口：** `main.ts`（引导器：StarGraph + Workspace + 面板单例 + 动作注册 + `createRoot(<App/>)`）· `workspace.ts`（项目状态机）

## 新架构（src/app/，重构落点）

| 文件 | 职责 |
|---|---|
| `app/App.tsx` | 单 React 根：CommandBar/DockRail/StatusBar/CommandPalette(Ctrl+K)/ShortcutsOverlay/ChatBeacon |
| `app/shell-store.ts` | chrome 唯一数据源（zustand） |
| `app/actions.ts` | 动作注册表（React 认 id，main.ts 注入实现） |
| `app/bridge-adapters.ts` | bus→store 适配（bus 冻结，app/** 不 import events.ts） |
| `app/chat/chat-core.ts` | 无头 ChatCore：会话/流式/权限/goal 编排（原 ui/chat.ts） |
| `app/chat/ChatBeacon.tsx` | 观测信标视图：pill→input→panel→hud 底部居中浮标 |

## 保留逻辑层（勿动）

`ui/chat-session.ts`（会话 CRUD/持久化）· `ui/chat-stream.ts`（Agent 事件→消息 store）· `ui/part-mutator.ts` · `agent/execution-state.ts` · `ui/graph.ts`（3657 行，**layout3D 参数锁定**，P4 拆解对象）

## Agent ↔ 星图双向联动

- Agent 调工具 → `agent:tool-done` → AgentVisualizer 高亮节点
- Shift+双节点 → 路径 → Agent 自动分析依赖链
- Alt+框选 → 区域总结
- 6 个面板均有「问 Agent」入口（经 actions 注册表）

## 渲染模式

`minimal` / `standard` / `full` — 独立 StarGraph 实例，切换即重建。折叠视图三层：宇宙/星座/Galaxy 内部。

## 数据加载

优先 MessagePack (`.hologram`)，fallback JSON。布局永远走 JS `layout3D()`（Python 预计算布局已删除）。

## 前端改动注意

- chrome 样式只写 `app/tokens.css` 的 `--obs-*` 变量；旧 `base.css/chat.css/panels.css` 随重构退役（P5 删除，聊天类契约在删前须搬走）
- 字体已自托管（`app/fonts.ts` fontsource），无 CDN 依赖
- 改前端后：`cd src-ui && npm run build` 再 `cargo tauri build`；门禁还要 `npx vitest run` + `npx biome ci src/app`
- `bridge.ts` 的 `invoke`/`listen` 封装 Tauri；`isMockMode` 用于无壳调试（浏览器 mock 可完整启动，支持无头截图验证）
